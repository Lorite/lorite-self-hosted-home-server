# User configuration
HOMELAB_USER=<your_username>
HOMELAB_DIR=/home/<your_username>/homelab
SERVICES_DIR=/home/<your_username>/homelab/services

# Pi-hole configuration
# Set this to your server's primary IP address (run: hostname -I | awk '{print $1}')
PIHOLE_HOST_IP=192.168.1.100
# Set a strong password for the Pi-hole web interface
PIHOLE_PASSWORD=changeme

# Domain configuration
DOMAIN=homelab.local

# Deployment mode
LOCAL_MODE=true

# Immich configuration
# The location where your uploaded files are stored (relative to docker-compose directory or absolute path)
IMMICH_UPLOAD_LOCATION=./library
# Timezone for Immich (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List)
IMMICH_TZ=America/New_York
# Immich version (use 'release' for latest or pin to specific version like 'v2.1.0')
IMMICH_VERSION=release
# Database password - CHANGE THIS to a strong random password
IMMICH_DB_PASSWORD=changeme
# Database configuration (usually no need to change these)
IMMICH_DB_USERNAME=postgres
IMMICH_DB_DATABASE_NAME=immich